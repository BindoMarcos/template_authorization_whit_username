# authorization_whit_username

Esto es un template de autenticacion y autorizacion (de rutas) para a una api (se podria dividir en 2 MVC por un lado y APIrest por el otro (para hacerla microservicio))

